# Chat memory (baseline)

This branch extends **`feat/memory`**. For file-backed history and message windowing, see the same topics in `feat/memory` (`TUTORIAL-MEMORY.md` there).

**On this branch**, read [TUTORIAL-MEMORY-SUMMARIZATION.md](TUTORIAL-MEMORY-SUMMARIZATION.md) for rolling session summaries when the window is exceeded.
