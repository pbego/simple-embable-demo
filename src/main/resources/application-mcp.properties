# Profile: mcp — consume external MCP tools (tutorial 21)
spring.config.activate.on-profile=mcp

spring.ai.mcp.client.type=SYNC
spring.ai.mcp.client.stdio.connections.filesystem.command=npx
spring.ai.mcp.client.stdio.connections.filesystem.args=-y,@modelcontextprotocol/server-filesystem,${user.home}

# Optional GitHub MCP (requires GITHUB_TOKEN and Docker):
# spring.ai.mcp.client.stdio.connections.github.command=docker
# spring.ai.mcp.client.stdio.connections.github.args=run,-i,--rm,-e,GITHUB_TOKEN,mcp/github
# spring.ai.mcp.client.stdio.connections.github.env.GITHUB_TOKEN=${GITHUB_TOKEN}
